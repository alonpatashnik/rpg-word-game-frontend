import React from 'react';
import ReactDOM from 'react-dom';
// not sure where to import the game
// import { GameEngine } from "react-game-engine";
import App from './App';

ReactDOM.render(
    <React.StrictMode>
        <App />
    </React.StrictMode>,
    document.getElementById('root')
);